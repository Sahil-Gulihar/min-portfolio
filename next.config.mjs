/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["cionews.co.in"], // Add the domain here as part of the 'domains' array
  },
};

export default nextConfig;
